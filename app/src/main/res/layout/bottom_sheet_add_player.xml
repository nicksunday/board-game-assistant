<?xml version="1.0" encoding="utf-8"?>
<!-- Portions of this layout were developed with the assistance of OpenAI's ChatGPT -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add New Player"
        android:textSize="18sp"
        android:textStyle="bold"
        android:paddingBottom="12dp" />

    <EditText
        android:id="@+id/playerNameInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Player Name"
        android:inputType="textPersonName"
        android:maxLines="1"
        android:imeOptions="actionNext" />

    <EditText
        android:id="@+id/playerFriendCodeInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Friend Code (optional)"
        android:textAllCaps="true"
        android:fontFamily="monospace"
        android:textAppearance="?attr/textAppearanceBody1"
        android:inputType="text"
        android:maxLines="1"
        android:imeOptions="actionDone"
        android:layout_marginTop="8dp" />

    <Button
        android:id="@+id/addPlayerConfirmBtn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add Player"
        android:layout_marginTop="16dp"/>
</LinearLayout>
